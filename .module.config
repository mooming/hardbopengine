name = HardbopEngine
buildType = None 
precompileDefinitions =
ignoreSubdirectories = build docs Tools Scripts

# Circular dependency resolution using linker group flags
linkerGroup = true
# Group only the Engine internal dependencies that may have circular deps
linkerGroupDependency = Core, Math, OSAL, Renderer, Resource, String

